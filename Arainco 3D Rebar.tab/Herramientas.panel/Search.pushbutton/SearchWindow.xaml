<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Buscador de proyecto" Height="230" Width="380" 
        WindowStartupLocation="CenterScreen"
        Topmost="True" ResizeMode="NoResize"
        Background="#F9F9F9">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <StackPanel Margin="20">
        <TextBlock Text="Buscar texto en todas las láminas del proyecto:" FontWeight="SemiBold" Margin="0,0,0,10"/>
        <TextBox x:Name="SearchTextBox" Height="25" Margin="0,0,0,15" Padding="2" FontSize="13"/>
        <CheckBox x:Name="MatchCaseCheckBox" Content="Distinguir mayúsculas y minúsculas" Margin="0,0,0,8"/>
        <CheckBox x:Name="WholeWordCheckBox" Content="Coincidir palabra completa" Margin="0,0,0,20"/>
        <Button x:Name="SearchButton" Content="Buscar" Height="30" Width="100" HorizontalAlignment="Right" Click="search_click" IsDefault="True"/>
    </StackPanel>
</Window>
